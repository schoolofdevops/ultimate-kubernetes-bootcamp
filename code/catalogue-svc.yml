apiVersion: v1
kind: Service
metadata:
  name: catalogue
  namespace: instavote
  labels:
    app: catalogue
    env: dev
spec:
  selector:
    app: catalogue
  ports:
    - protocol: TCP
      port: 80
      
     
